---
paths:
  - "tests/**/*.test.ts"
  - "src/**/*.test.ts"
---

# テストルール

- テスト名は日本語で「〜すること」の形式で書く
- Arrange-Act-Assert パターンに従う
- モックは最小限にし、可能な限り実際の依存を使う
- テストデータは各テストケース内で定義する（共有しない）
- スナップショットテストは避け、具体的なアサーションを書く
